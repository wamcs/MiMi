<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    
    <android.support.v7.widget.Toolbar
        android:id="@id/tool_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/default_toolbar_height">
        <include layout="@layout/layout_default_toolbar"/>
    </android.support.v7.widget.Toolbar>

    <FrameLayout
        android:layout_below="@id/tool_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/post_event_activity_bottom_button"
        android:id="@+id/activity_post_event_container"/>

    <Button
        android:layout_width="match_parent"
        android:id="@id/post_event_activity_bottom_button"
        android:background="@color/basic_orange"
        android:layout_height="@dimen/post_event_bottom_button"
        android:layout_alignParentBottom="true"/>




</RelativeLayout>